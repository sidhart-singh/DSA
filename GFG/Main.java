package GFG;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.*;

public class Main {

    public static void main(String[] args) throws Exception {
        // new ClassTest().test();
        // new FindingCoolest().test();
        // new LoveAndThunder().test();

        // BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        // String str = br.readLine();

        // int result = 0;
        // HashMap<Character, Integer> m = new HashMap<>();
        // for (int i = 0; i < str.length(); i++) {
        // if (m.containsKey(str.charAt(i)))
        // m.put(str.charAt(i), m.getOrDefault(str.charAt(i), 0));
        // }

        // for (Map.Entry<Character, Integer> e : m.entrySet()) {
        // if (e.getValue() == 2)
        // result++;
        // }

        // System.out.println(result);

        // LongestBeautifulSubsequenceII lbsii = new LongestBeautifulSubsequenceII();
        // lbsii.longestBeautifulSubsequence(4, new int[] { 2, 2, 6, 2 });
    }

}
